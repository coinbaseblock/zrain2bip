# zrain2bip

zrain2bip

## Standalone HTML

For running on environments like Koder on iPhone without extra assets, use `index-standalone.html`.
This file has all CSS and JavaScript inlined, so it can run as a single self-contained HTML file.
